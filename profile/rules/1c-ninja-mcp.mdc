---
description: MCP 1c-ninja-mcp — static + live только из project .cursor/mcp.json открытого workspace
alwaysApply: true
---

# 1c-ninja-mcp: static + live

## Scope

- **Project** `<репозиторий>\.cursor\mcp.json` (gitignore) — полный MCP этой ИБ, в т.ч. ninja с `NINJA_URL` / `NINJA_USER` / `NINJA_PASSWORD`. Эталон: скилл `1c-env-setup` (`mcp.json.example.tpl`).
- **User** `%USERPROFILE%\.cursor\mcp.json` — **без** `1c-ninja-mcp` и прочих 1С-серверов (иначе дубли с проектом). Live-URL в user не класть.
- Не подставляй URL другой базы «по умолчанию». Нет project env → явный `url` у тула или отказ, не угадывание.

## Живые данные ИБ (приоритет)

Нужны **реальные данные / запрос / метаданные ИБ / журнал / eval** — сначала **1c-ninja-mcp** `live_*` (публикация Apache + расширение `NinjaLive` в ИБ, RootURL `ninja-live`).

| Задача | Tool |
|--------|------|
| Smoke / версия сервиса | `live_version` |
| Запрос SELECT | `live_query` |
| Проверка текста запроса | `live_validate_query` |
| Синтаксис BSL без выполнения | `live_check_syntax` |
| Список / структура метаданных ИБ | `live_metadata_list`, `live_metadata_structure` |
| Журнал регистрации | `live_event_log` |
| Список расширений ИБ (**главный способ**) | `live_extensions_list` (не `vrunner infobase extensions list` / MCP `extensions_list` — запуск Предприятия; не `extensions_dump_list` для обзора) |
| Выполнить код / выражение | `live_execute`, `live_eval` (осторожно; роль `NinjaLive_ВыполнениеКода`) |

**Главный способ списка CFE в ИБ:** `live_extensions_list` после `src/cfe/NinjaLive` → `cfe_load` → публикация. Setup: скилл `1c-env-setup`. **Не** запускать Предприятие ради списка.

**1c-mcp-toolkit** — только если live ninja недоступен: нет публикации `/hs/ninja-live`, расширение `NinjaLive` нельзя подключить, или `live_*` стабильно падает. Toolkit: `MCP_Toolkit.epf` на `:6003`.

Граф вызовов / `symbol_info` / диагностики по файлам — правило `bsl-analyzer`, не ninja live и не grep.

## Поиск по выгрузке (static)

`bsl_search`, `xml_search`, `config_list`, `read_module`, `syntax_help_search` — `path` = абсолютный путь **этого** проекта. Grep/Read — точечно или если MCP недоступен.

Загрузка / хранилище / syntax-check — **vrunner** (`1c-development-process`). Разбор EPF/ERF — **1c-ninja-mcp** `epf_decompile` (не голый vrunner). Скиллы: `1c-ninja-mcp`, `1c-mcp-toolkit`.
